- if @restaurants.any?
  - @restaurants.each do |restaurant|
    %h3
      Review for: #{restaurant.name}
    - if restaurant.reviews.any?
      %ul
        - restaurant.reviews.each do |review|
          %li
            = review.thoughts
            %strong= review.rating
            \/5
    - else
      %p No reviews

    = link_to restaurant.name, restaurant_path(restaurant)

    - if current_user && current_user.id == restaurant.user_id
      = link_to("Edit #{restaurant.name}", edit_restaurant_path(restaurant))
    = link_to("Delete #{restaurant.name}", restaurant_path(restaurant), method: :delete)
    = link_to("Review #{restaurant.name}", new_restaurant_review_path(restaurant))

- else
  %h1 No restaurants yet!

- if current_user
  = link_to("Add a restaurant", new_user_restaurant_path(current_user.id))
  / %a(href=new_user_restaurant_path(current_user.user_id)) Add a restaurant
